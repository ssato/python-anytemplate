[tox]
envlist = py27, py34, py35, py36, py37

# TODO:
#toxworkdir = /tmp/.tox

[flake8]
exclude = .git,.tox,dist,*egg,setup.py

[testenv]
deps = -r{toxinidir}/pkg/test_requirements.txt
#commands = bash pkg/runtest.sh
#commands = nosetests -v --with-doctest --all-modules --where tests
commands = python -m nose -v --with-doctest --all-modules --where tests
#whitelist_externals =
#  /bin/bash
#  /usr/bin/bash
#  /usr/bin/test
setenv =
    PYTHONPATH = {toxinidir}/src

[testenv:py27]
deps = -r{toxinidir}/pkg/test_requirements-py-2.7.txt
